import 'package:flutter/material.dart';

class AppColors {
  static const Color seed = Color(0xFF6750A4);

  // Semantic colors (minimal set).
  static const Color primary = seed;
  static const Color secondary = Color(0xFF625B71);

  static const Color background = Color(0xFFFFFBFE);
  static const Color surface = Color(0xFFFFFFFF);

  static const Color textPrimary = Color(0xFF1C1B1F);
  static const Color textSecondary = Color(0xFF49454F);
  static const Color subtleBorder = Color(0xFFE6E6E6);

  // Patients screen tokens (minimal, reusable).
  static const Color bgCanvas = Color(0xFFF3F4F6);
  static const Color cardSurface = Color(0xFFFFFFFF);

  static const Color chipActiveBg = Color(0xFFE6F4F1);
  static const Color chipActiveFg = Color(0xFF2A9D8F);
  static const Color chipInactiveBg = Color(0xFFFDECEC);
  static const Color chipInactiveFg = Color(0xFFE04F5F);

  static const Color badge = Color(0xFFFF4D4D);

  static const Color success = Color(0xFF2E7D32);
  static const Color warning = Color(0xFFED6C02);
  static const Color error = Color(0xFFD32F2F);
}
